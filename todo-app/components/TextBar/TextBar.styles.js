import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: { backgroundColor: '#e0e0e0', padding: 10, borderRadius: 5 },
  input: { borderBottomWidth: 1, borderColor: '#fff' },
  button: { margin: 10, alignItems: 'center', backgroundColor: '#aaa', padding: 10, borderRadius: 8,},
  button_text: { color: '#fff'}
});